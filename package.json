{
  "name": "cebolinha-ipsum",
  "version": "1.0.0",
  "private": true,
  "description": "Um `Lorem ipsum...` com um leve toque brasileiro.",
  "scripts": {
    "build": "rm -rf dist/* && parcel build index.html --public-url .",
    "build:dev": "parcel index.html",
    "deploy": "npm run build && surge -d https://cebolinhaipsum.surge.sh -p dist"
  },
  "author": "Matheus Alves",
  "license": "MIT",
  "devDependencies": {
    "cebolinha": "0.0.7",
    "lorem-ipsum": "^1.0.4",
    "normalize.css": "^8.0.0",
    "parcel-bundler": "^1.6.1",
    "scroll": "github:theuves/scroll",
    "surge": "^0.19.0"
  }
}
